#!/bin/bash

module load PrgEnv-gnu
module load curl zlib

NETCDF_ROOT=/ccs/home/imn/software/netcdf_spock_gnu
YAML_ROOT=/ccs/home/imn/software/yaml_cpp_spock_gnu

unset YAKL_ARCH

export CXX=g++

export YAKL_HOME=../../../externals/YAKL
unset CXXFLAGS
export YAKL_CXX_FLAGS="-O3 `${NETCDF_ROOT}/bin/ncxx4-config --cflags` -I$YAML_ROOT/include"
export NCFLAGS="`${NETCDF_ROOT}/bin/ncxx4-config --libs` `${NETCDF_ROOT}/bin/nc-config --libs` -L$YAML_ROOT/lib64 -lyaml-cpp"


